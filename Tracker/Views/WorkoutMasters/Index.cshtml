@model IEnumerable<Tracker.Models.TrackerModels.WorkoutMaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkoutName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WorkoutName)
        </td>
        <td>
            @Html.ActionLink("Add to Workout", "Create", "Workouts") |
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkoutMasterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkoutMasterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkoutMasterID })
        </td>
    </tr>
}

</table>
